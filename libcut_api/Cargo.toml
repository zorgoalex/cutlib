[package]
name = "libcut_api"
version = "0.1.0"
edition = "2021"

[dependencies]
libcut_core = { path = "../libcut_core" }
serde_json = "1"
axum = "0.8"
tokio = { version = "1", features = ["full"] }
